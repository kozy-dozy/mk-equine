// SpotlightDto: DTO for storing spotlight player IDs by gender
export interface SpotlightDto {
    male: string[] // Array of player IDs (max 3)
    female: string[] // Array of player IDs (max 3)
}
